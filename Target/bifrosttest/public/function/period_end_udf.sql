CREATE OR REPLACE FUNCTION BIFROSTTEST.PUBLIC.PERIOD_END_UDF(PERIOD_VAL VARCHAR(22))
RETURNS TIMESTAMP_LTZ(9)
LANGUAGE SQL
IMMUTABLE
AS 
$$
 CAST(SUBSTR(PERIOD_VAL,POSITION('*',PERIOD_VAL)+1) AS TIMESTAMP)
$$;
