create or replace TABLE BIFROSTTEST.PUBLIC.EMP (
	EMPNO NUMBER(5,0) NOT NULL,
	NAME VARCHAR(20),
	SAL NUMBER(10,2),
	JOB VARCHAR(20),
	MGR NUMBER(5,0),
	HIREDATE DATE,
	COMM NUMBER(10,2),
	primary key (EMPNO)
);
